# never manage rstudio config
.config/rstudio

# mac-only configs — ignore on non-mac systems
{{ if ne .chezmoi.os "darwin" }}
.config/raycast
.config/iterm2
{{ end }}

# repo-level files — not dotfiles, should not be deployed to ~
README.md
LICENSE
Brewfile
Brewfile.linux
bootstrap.sh
packages
scripts/
